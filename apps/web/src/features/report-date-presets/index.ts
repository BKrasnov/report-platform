import { buildDatePresets } from './model';
import { ReportDatePresetButtons } from './ui';

export const reportDatePresetsModel = {
  utils: {
    buildDatePresets,
  },
};

export const ReportDatePresets = {
  Buttons: ReportDatePresetButtons,
};
